import { describe, it } from "vitest";

describe("find_differences", () => {
  it.todo("detects attribute modifications");
  it.todo("detects tag replacements (removed + added)");
  it.todo("detects text modifications");
  it.todo("detects added children");
  it.todo("detects removed children");
});
