import { describe, it } from "vitest";

describe("merge_trees", () => {
  it.todo("merges attributes with tree2 taking precedence");
  it.todo("merges children deeply when identities align");
  it.todo("replaces children when identities do not align");
  it.todo("handles text vs element replacement");
});
